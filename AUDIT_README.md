This zip is the Tabula Medica web repo audit snapshot from 2026-04-18.

Start with `AUDIT_CONTEXT.md` for what to focus on — it ranks the five
areas most worth a fresh set of eyes, lists known open Action Items,
and flags ten codebase patterns that look unusual but are intentional.

Ignore anything in `/attached_assets/` if it leaked in (it shouldn't have —
that path was excluded at zip time).

Known open items:
- Action Item A — four `/legal/*` pages return 404; user is sourcing copy
  from Termly / Common Paper / lawyer in parallel.
- Action Item D — `seed:reviewer-account` npm alias in `package.json`;
  resolved in this zip if the user's manual edit succeeded (verifiable via
  `grep "seed:reviewer-account" package.json`).
- Mac-side build work — described in `.local/deliverables/capacitor-build-playbook.md`.
  The `ios/` and `android/` directories deliberately do not exist yet;
  they are generated by `npx cap add` on a Mac.

Pre-prompt components (`<HealthKitConsentDialog />`, `<MicRecordingExplainer />`)
ship in this build but call sites are deliberately not wired — see
`AUDIT_CONTEXT.md` §2.C for why.
